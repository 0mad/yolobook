import PageTemplate from '../components/common/PageTemplate';

interface IProps {}

const About = (props: IProps) => (
  <PageTemplate>
    <div>about 페이지</div>
  </PageTemplate>
);

export default About;
