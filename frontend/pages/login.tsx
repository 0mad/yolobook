import PageTemplate from '../components/common/PageTemplate';

interface IProps {}

const Login = (props: IProps) => (
  <PageTemplate>
    <div>login 페이지</div>
  </PageTemplate>
);

export default Login;
